import { version } from '../../../../package.json';

export const swaggerConfig = {
  title: 'Hero Manager API',
  description: 'API de gerenciamento de heróis',
  version,
  tag: 'hero-manager',
};
